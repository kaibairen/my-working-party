# AI 办公室 IA（M2 · 决策人主路径）

对齐 `DECISION_MAKER_CLICK_FLOW_v1` §4。默认 `/` 与 `/inbox` 是同一壳：**AI 办公室 + 待我拍板**。

- 顶栏：`AI 办公室` · `查看待我拍板` / `待我拍板 · N` · 弱 `工位一览`
- 空态：§1 步骤 2（安静；不去画布 / Roster）
- 卡面：人话标题、相对时间、`还不能过，缺这些`、authority 角标 `硬门禁`
- 按钮：`通过` / `打回重做` / `以后再说`；`version` 静默；409：`别人刚拍过，已为你刷新`
- 文案包：`apps/web/copy/zh-DM.ts`
- Ops / Health / OpenAPI：仅 `/ops` 或 `?dev=1`

Roster / 填充槽可写 UI / 画布派发前置 = 非本 PR。
